extends layouts/main
include mixins/videoBlock

block content
    .vidieos
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                views: item.views,
                videoFile: item.fileUrl
            })